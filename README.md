# dotfiles-bootstrap

Tiny public entrypoint for a fresh machine.

```bash
sh -c "$(curl -fsSL https://raw.githubusercontent.com/tumpaksewu/dotfiles-bootstrap/main/bootstrap.sh)"
```

Installs:

- GitHub CLI (`gh`)
- DotState

Then prints the private-repo login/clone steps.

The private dotfiles repo stays private.
